# Tenant Isolation Configuration
# Copy this file to tenant-isolation.env and customize for your environment

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=factorydb
DB_USER=factoryadmin
DB_PASSWORD=localpass

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=saas-factory-prod
CLOUD_RUN_REGION=us-central1

# Container Image Configuration
# This should point to your Cloud Run container image
CONTAINER_IMAGE=us-central1-docker.pkg.dev/saas-factory-prod/saas-factory/api:latest

# Authentication (for gcloud CLI)
# Ensure you are authenticated with: gcloud auth login
# And have the necessary permissions:
# - Cloud Run Admin
# - Cloud SQL Admin  
# - Service Account User

# Optional: Custom VPC Connector (if different from default)
# VPC_CONNECTOR_NAME=vpc-connector

# Optional: Custom service account for Cloud Run
# SERVICE_ACCOUNT_EMAIL=cloud-run-sa@saas-factory-prod.iam.gserviceaccount.com

# Load this file with: source config/environments/tenant-isolation.env 
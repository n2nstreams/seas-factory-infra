# Core web framework
Flask>=3.0.0

# Google Cloud dependencies
google-cloud-pubsub>=2.20.0
google-cloud-firestore>=2.20.0
google-auth>=2.0.0

# Core Python dependencies
python-dotenv>=1.0.0
requests>=2.25.0

# Remove conflicting autogen and AI platform dependencies for now
# We can add them back gradually once the core Pub/Sub functionality works
